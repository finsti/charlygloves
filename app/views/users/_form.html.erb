<%= form_with(model: user, local: true) do |form| %>

  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name, "Vorname", class: "form__label" %>
    <%= form.text_field :first_name, class: "form__text-field" %>
  </div>

  <div class="field">
    <%= form.label :last_name, "Nachname", class: "form__label" %>
    <%= form.text_field :last_name, class: "form__text-field" %>
  </div>

  <div class="field">
    <%= form.label :email, "E-Mail", class: "form__label" %>
    <%= form.text_field :email, class: "form__text-field" %>
  </div>

  <p><%= form.submit "absenden", class: "form__btn form__btn--primary" %></p>

<% end %>
