<% if signed_in? %>
  <div class="container--small center">
    <h2>Bewertung hinzufügen</h2>
    <p>
      <%= form_for([@product, @product.comments.build]) do |f| %>
        <div class="field">
          <%= f.label :body, "Kommentar", class: "form__label" %>
          <%= f.text_area :body, class: "form__text-field" %>
        </div>
        <p>
          <div class="rating"></div>
        </p>
        <p><%= f.submit "Absenden", class: "form__btn form__btn--primary" %></p>
      <% end %>
    </p>
  </div>
<% end %>
